# ── Root .htaccess: redirect everything to /public ────────────────────
# Required when running in a XAMPP subfolder (e.g. htdocs/jk/)
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>
